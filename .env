# ============================================================================
# LyricDisplay environment configuration
# Copy this file to .env.local for per-machine overrides if needed.
# ============================================================================

# General runtime settings ---------------------------------------------------
NODE_ENV=development
PORT=4000
CONFIG_PATH=./config

# Authentication token defaults (server/index.js) ---------------------------
TOKEN_EXPIRY=24h
ADMIN_TOKEN_EXPIRY=7d
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50

# Desktop compatibility toggle (main.js) ------------------------------------
FORCE_COMPATIBILITY=

# Vite/renderer configuration ----------------------------------------------
VITE_SOCKET_SERVER_URL=http://localhost:4000
VITE_ADMIN_KEY=
VITE_ENABLE_VERBOSE_LOGS=false
